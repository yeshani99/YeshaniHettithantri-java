package com.mycompany.lab06;

public interface MyFirstInterface 
{
    int x = 10;
    // final public static int x=10;
    // All the variables declared inside an interface, by default final public static whether it is implicitly stated or not.  
    void display();
    // abstract void display();
    // All the methods declares inside an interface, all are abstract whether it is implicitly or not. 
}
